const ScoreButton = ({ score }) => {
    return (
        <>
        <button className='btn' style={{ backgroundColor: 'yellowgreen' }}>
            {score}
        </button>
        
        </>
    )
}

export default ScoreButton
